[project]
name = "hack-poc"
version = "0.1.0"
description = "MCP server for automated coding task video generation using Manim + Kokoro TTS"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.26.0",
    "numpy>=2.0.0",
    "soundfile>=0.13.0",
    "kokoro>=0.9.0",
]
